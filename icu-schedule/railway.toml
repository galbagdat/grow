[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx --yes serve -s . -l ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
